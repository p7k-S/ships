#pragma once
#include <vector>
#include <stack>
#include <unordered_set>

#include "GameLogic.h" // Где объявлен GameLogic::Hex и CellType
// #include "map.h" // Где объявлен GameLogic::Hex и CellType

namespace GameLogic {
    inline void detectIsland(const std::vector<std::vector<Hex>>& map) {
        for (auto& hex : map) {
        }
    }
} // namespace GameLogic
